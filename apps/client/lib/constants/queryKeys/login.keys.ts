const loginWithCredential = ['login With Credential']
const loginWithGoogle = ['login With Google']
const register = ['register an user']
const resetPassword = ['reset password']
const requestPasswordReset = ['request password reset']
const reAuth = ['reAuth']

export const LOGIN_KEYS = {
  loginWithCredential,
  loginWithGoogle,
  register,
  resetPassword,
  requestPasswordReset,
  reAuth
}
